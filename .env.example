# Example-only local configuration. Copy to .env and choose a local password.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=delivery_pulse
POSTGRES_USER=delivery_pulse
POSTGRES_PASSWORD=replace_with_local_password
POSTGRES_SCHEMA=delivery_pulse
METABASE_PORT=3000
